// import Profile model
const { Profile } = require('../models');

// import sign token function from auth
const { signToken } = require('../utils/auth');
    
    // pulling a contact through the profile by either their last name or

